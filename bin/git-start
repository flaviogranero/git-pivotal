#!/usr/bin/env ruby

$LOAD_PATH.unshift File.join(File.dirname(__FILE__), '..', 'lib')

require 'git-pivotal/commands/start'

args = ARGV
exit GitPivotal::Commands::Start.for(*ARGV).with(STDIN, STDOUT).run!
